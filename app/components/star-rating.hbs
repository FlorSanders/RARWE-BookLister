{{#each this.stars as |star|}}
<button type="button" {{on "click" (fn @onUpdate star.rating)}}>
    {{!-- This inline if works equivalently to the ternary operator in JavaScript --}}
    <FaIcon @icon='star' @prefix={{if star.full 'fas' 'far' }} />
</button>

{{/each}}